# 本地通知与后台闹钟设计

## 目标

绸缪需要在事件到点时完成三件事：

1. 发出本地通知
2. 展示事件标题、备注、表情包和语音包信息
3. 在允许的情况下播放提示音或启动提醒页

## Android 设计

建议使用以下能力组合：

- `AlarmManager`：用于精确闹钟唤醒
- `BroadcastReceiver`：用于接收闹钟触发事件
- `NotificationChannel`：用于 Android 8+ 通知通道
- `Foreground Service`：在需要长时间播放音频或后台保活时使用
- `WorkManager`：用于非精确、可延迟的后台同步任务

### Android 流程

1. 用户保存事件
2. 计算触发时间
3. 注册本地闹钟
4. 闹钟触发后发送通知
5. 打开提醒详情页或播放提示音

## iOS 设计

建议使用以下能力组合：

- `UNUserNotificationCenter`：本地通知
- `UNNotificationRequest`：定时触发通知
- `UNNotificationCategory`：支持通知按钮，例如“完成”“延后”
- `Background Tasks`：用于有限后台任务
- 音频播放能力：用于前台或部分后台场景的提醒音

### iOS 流程

1. 用户保存事件
2. 申请通知权限
3. 创建定时通知请求
4. 到点推送本地通知
5. 通过通知操作进入事件详情或完成状态

## Capacitor 接入建议

可把通知和闹钟能力拆成两个插件层：

- 通知插件：负责展示提醒
- 闹钟插件：负责原生闹钟注册与唤醒

在当前项目里，前端页面只负责：

- 选择事件
- 选择提示音
- 选择表情包
- 调用原生能力保存提醒

## 备注

真正的“后台持续播放闹钟”在 Android 和 iOS 上策略不同。最稳妥的方式是：

- 以本地通知为主
- 以原生闹钟唤醒为辅
- 以前台播放/通知点击进入详情页来完成完整提醒体验
