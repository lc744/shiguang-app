// WXS：WXML 表达式不支持方法调用，用模块函数替代 Array.indexOf（多选勾选判定）
module.exports = {
  has: function(arr, id){
    if(!arr) return false;
    for (var i = 0; i < arr.length; i++) { if (arr[i] === id) return true; }
    return false;
  }
};
